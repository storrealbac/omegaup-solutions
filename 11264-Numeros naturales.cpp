#include<bits/stdc++.h>

using namespace std;

int main() {
    
    int from, to;
    scanf("%d%d", &from, &to);

    for (int i = from; i <= to; i++)
        printf("%d ", i);
    printf("\n");

    return 0;
}